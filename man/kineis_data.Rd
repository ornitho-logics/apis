% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kineis.R
\name{kineis_data}
\alias{kineis_data}
\title{Download Kineis telemetry for a time interval ----}
\usage{
kineis_data(
  token,
  api_telemetry_url,
  datetime = "2000-01-01T00:00:00.000Z",
  end_datetime = NULL,
  device_refs = character(),
  page_size = 100L,
  retrieve_metadata = TRUE,
  retrieve_raw_data = TRUE,
  retrieve_doppler = TRUE,
  retrieve_gps_loc = TRUE,
  retrieve_sensors = TRUE,
  retrieve_additional_properties = TRUE,
  datetime_format = "DATETIME",
  verbose = interactive(),
  page_handler = NULL,
  collect = TRUE
)
}
\arguments{
\item{token}{Access-token string, the full result of \code{\link[=kineis_login]{kineis_login()}}, or a
cached token-provider function accepting a logical \code{force} argument.}

\item{api_telemetry_url}{Kineis telemetry API base URL.}

\item{datetime}{Inclusive start time in UTC, formatted as RFC 3339 text.}

\item{end_datetime}{Optional end time in UTC, formatted as RFC 3339 text.
When omitted, the API uses the current time.}

\item{device_refs}{Character vector of device references. An empty vector
retrieves every device available to the login profile.}

\item{page_size}{Number of records requested per page.}

\item{retrieve_metadata, retrieve_raw_data, retrieve_doppler, retrieve_gps_loc, retrieve_sensors}{Logical
flags selecting message fields.}

\item{retrieve_additional_properties}{Include domains and program
properties. This maps to the API's misspelled
\code{retrieveAdditionnalProperties} field.}

\item{datetime_format}{Response timestamp format. Defaults to \code{"DATETIME"}.}

\item{verbose}{Print the \link[httr2:request]{httr2::request} objects. Defaults to
\code{\link[=interactive]{interactive()}}.}

\item{page_handler}{Optional function called with each non-empty page before
the next page is requested. This permits incremental database writes.}

\item{collect}{Retain and return all downloaded pages. Set to \code{FALSE} when a
\code{page_handler} persists each page and the combined result is not needed.}
}
\value{
A data.table with one row per telemetry message.
}
\description{
Uses the bulk endpoint and follows \code{pageInfo$endCursor} until every page in
the requested interval has been retrieved. All fields returned by the API
are preserved. Nested metadata and sensor fields are flattened with their
parent name (for example, \code{kineisMetadata.sat} and \code{sensors.TEMP}). Identifier
columns use character storage so 64-bit values remain exact.
Messages are requested in ascending \code{msgDatetime} order. Requests are paced
to one per five seconds. HTTP 429 and 503 responses are retried
up to eight times within ten minutes, honoring the server's \code{Retry-After}
header when present.
}
\examples{
\dontrun{
crd <- config::get(config = "kineis_api")
token <- kineis_login(crd$un, crd$pwd, crd$auth_url)
telemetry <- kineis_data(
  token,
  crd$api_telemetry_url,
  datetime = "2026-07-01T00:00:00.000Z",
  end_datetime = "2026-07-02T00:00:00.000Z"
)
}
}
