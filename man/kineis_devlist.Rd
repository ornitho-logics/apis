% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kineis.R
\name{kineis_devlist}
\alias{kineis_devlist}
\title{List all Kineis devices for the login profile ----}
\usage{
kineis_devlist(token, api_telemetry_url, verbose = interactive())
}
\arguments{
\item{token}{Access-token string, the full result of \code{\link[=kineis_login]{kineis_login()}}, or a
cached token-provider function accepting a logical \code{force} argument.}

\item{api_telemetry_url}{Kineis telemetry API base URL.}

\item{verbose}{Print the \link[httr2:request]{httr2::request} object. Defaults to
\code{\link[=interactive]{interactive()}}.}
}
\value{
A data.table containing all devices available to the login profile.
}
\description{
Requests are paced to one per five seconds. HTTP 429 and 503 responses are retried,
honoring the server's \code{Retry-After} header when present.
}
\examples{
\dontrun{
crd <- config::get(config = "kineis_api")
token <- kineis_login(crd$un, crd$pwd, crd$auth_url)
devices <- kineis_devlist(token, crd$api_telemetry_url)
}
}
